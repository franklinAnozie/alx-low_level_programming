#include <ctype.h>
/**
 * _isalpha - prints all possible combinations of two two-digit numbers
 * @c: the character to check
 * Return: 0 (Success)
 */
int _isalpha(int c)
{
return (isalpha(c) ? 1 : 0);
}
